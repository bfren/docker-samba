[<%= ${NAME} %>]
   comment = <% if [ "${COMMENT}" != "null" ] ; then echo "${COMMENT}" ; else echo "" ; fi %>
   path = /files/<%= "${NAME}" %>
   guest ok = yes
   force user = samba
   browsable = yes
   writable = yes
   create mask = 0666
   directory mask = 0777
