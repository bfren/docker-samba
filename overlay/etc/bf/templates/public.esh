[<%= ${NAME} %>]
   <% if [ -n "${COMMENT-}" ] ; then %>comment = <%= "${COMMENT}" %><% fi %>
   path = /files/<%= "${NAME}" %>
   guest ok = yes
   force user = samba
   browsable = yes
   writable = yes
   create mask = 0666
   directory mask = 0777
