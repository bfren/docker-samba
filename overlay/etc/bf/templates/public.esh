[<%= ${NAME} %>]
   comment = <% if [ "${COMMENT}" != "null" ] ; then printf "${COMMENT}" ; else printf "" ; fi %>
   path = /files/<%= "${NAME}" %>
   public = yes
   only guest = yes
   force user = samba
   browsable = yes
   writable = yes
   create mask = 0666
   directory mask = 0777
