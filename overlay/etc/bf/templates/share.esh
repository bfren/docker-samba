[<%= "${NAME}" %>]
   comment = <% if [ "${COMMENT}" != "null" ] ; then echo "${COMMENT}" ; else echo "" ; fi %>
   path = /files/<%= "${NAME}" %>
   valid users = <%= "${USERS}" %>
   force user = samba
   browsable = <%= "${BROWSEABLE}" %>
   writable = <%= "${WRITEABLE}" %>