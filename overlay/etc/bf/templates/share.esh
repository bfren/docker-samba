[<%= "${NAME}" %>]
   comment = <%= "${COMMENT}" %>
   path = /files/<%= "${NAME}" %>
   valid users = <%= "${USERS}" %>
   force user = samba
   browsable = <%= "${BROWSEABLE}" %>
   writable = <%= "${WRITEABLE}" %>