[<%= "${NAME}" %>]
   comment = <% if [ "${COMMENT}" != "null" ] ; then printf "${COMMENT}" ; else printf "" ; fi %>
   path = /files/<%= "${NAME}" %>
   valid users = <%= "${USERS}" %>
   force user = samba
   browsable = <%= "${BROWSEABLE}" %>
   writable = <%= "${WRITEABLE}" %>