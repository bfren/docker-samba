#!/usr/bin/nu

use bf
bf env load -x

# Attempt to connect to the local Samba server and list shares
def main [] {
    # capture output of drill executable
    bf write debug $"Requesting local shares."
    { ^smbclient -L \\localhost -U % -m SMB3 } | bf handle -c | exit $in
}
